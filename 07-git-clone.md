# Git Clone

The `git clone` command is used to **copy a project (repository) from a remote server** (like GitHub) to your local computer.  
It creates a **full copy of the project**, including all files and the complete version history.

---

## Syntax

```bash
git clone <repository_url>